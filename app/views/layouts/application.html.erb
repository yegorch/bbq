<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Bbq" %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_pack_tag "application", :media => "all" %>
    <%= javascript_pack_tag "application" %>
  </head>
  <body>

    <div class="py-2 bg-light border-bottom">
      <div class="container">
        <%= link_to "BBQ", root_path, class: 'navbar-brand' %>
        <div class="navbar-collapse">
          <ul class="nav me-auto">
            <% if user_signed_in? %>
            <li><%= link_to 'Профиль', current_user %></li>
            <%= link_to(destroy_user_session_path, {:class => "nav-item", :method => :delete}) do  %>
            <span class="sidebar-normal"> Выйти </span>
      <% end %>
            <% else %>
            <li><%= link_to 'Войти', new_user_session_path %></li>
            <li><%= link_to 'Зарегистрироваться',
                    new_user_registration_path %></li>
           <% end %>
          </ul>
        </div>
      </div>
    </div>

    <div class="container">
      <div class="row">
        <div class="col-lg-9">
          <%= yield %>
        </div>
        <div class="col-lg-3">
         
        </div><!--/span-->
      </div><!--/row-->

      <footer>
        <p>&copy; Company 2021</p>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
